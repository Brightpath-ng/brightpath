export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-4xl font-semibold text-(--text-primary)">BrightPath</h1>
      <p className="text-(--text-secondary)">Signed-in product shell — role routing lands with the auth feature.</p>
    </main>
  );
}
